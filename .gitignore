# The real config never enters the repo. It lives at
# ~/.config/flatline-roundtable/FlatlineRoundtable.yaml — this entry is a second
# belt in case someone copies it here anyway. It holds internal hostnames and
# pass entry names; the keys themselves are only ever in `pass`.
FlatlineRoundtable.yaml

# transcripts, if anyone points --config at a repo-local run
transcripts/

*.bak.*
*~
.DS_Store
__pycache__/
